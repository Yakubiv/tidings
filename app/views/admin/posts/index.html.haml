%h3 Posts

.table-responsive
  %table.table.table-striped
    %thead
      %tr
        %th Title
        %th Description
        %th Content
        %th
        %th

    %tbody
      - @posts.each do |post|
        %tr
          %td= post.title
          %td= post.description
          %td= post.content
          %td= link_to 'Edit', edit_admin_post_path(post)
          %td= link_to 'Destroy', [:admin, post], method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Post', new_admin_post_path, class: 'btn btn-secondary'
