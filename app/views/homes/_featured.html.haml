.entry{ style: "background-image: url(#{post.thumbnail_url(:big_thumb)})" }
  .entry__content
    %span.entry__category
      %a{ href: '' }= post.category
    %h1
      %a{ href: post_path(post) }
        = post.title
    .entry__info
      = post.description
